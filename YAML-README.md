### CI/CD Pipeline
This project includes an Azure DevOps YAML pipeline that:
- Installs Terraform
- Initializes a remote AzureRM backend
- Plans and applies Azure Consumption Budgets

The pipeline assumes an existing Azure DevOps service connection.
